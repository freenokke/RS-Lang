import Page from '../../../../helpers/page';

export default class GameButtonsWrapper extends Page {
  constructor(parentNode: HTMLElement) {
    super('div', ['wordsbook-games__wrapper'], parentNode, '', {});
  }
}
