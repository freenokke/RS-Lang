import Page from '../../../../helpers/page';

export default class WordsbookWordsWrapper extends Page {
  constructor(parentNode: HTMLElement) {
    super('div', ['wordsbook-words-wrapper'], parentNode, '', {});
  }
}
