export enum Domain {
  BASE = 'http://127.0.0.1:3000',
}

export enum Path {
  WORDS = 'words',
  USERS = 'users',
  TOKENS = 'tokens',
  AGWORDS = 'aggregatedWords',
  STAT = 'statistics',
  SETTINGS = 'settings',
  SIGNIN = 'signin',
}
