enum Pages {
  main = '#/main',
  about = '#/about',
  audiochallenge = '#/games/audiochallenge',
  sprint = '#/games/sprint',
  wordsbook = '#/wordsbook',
  games = '#/games',
}

export default Pages;
