enum Pages {
  main = '#/main',
  about = '#/about',
  audiochallenge = '#/games/audiochallenge',
  auth = '#/auth',
  registration = '#/reg',
  wordsbook = '#/wordsbook',
  games = '#/games',
}

export default Pages;
