enum Pages {
  main = '#/main',
  about = '#/about',
  audiochallenge = '#/games/audiochallenge',
  stats = '#/stats',
  sprint = '#/games/sprint',
  wordsbook = '#/wordsbook',
  games = '#/games',
}

export default Pages;
