enum Pages {
  main = '#/main',
  about = '#/about',
  audiochallenge = '#/games/audiochallenge',
  wordsbook = '#/wordsbook',
  auth = '#/auth',
  registration = '#/reg',
  stats = '#/stats',
  sprint = '#/games/sprint',
  games = '#/games',
}

export default Pages;
