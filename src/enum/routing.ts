enum Pages {
  main = '#/main',
  about = '#/about',
  audiochallenge = '#/games/audiochallenge',
  auth = '#/auth',
  registration = '#/reg',
  stats = '#/stats',
  sprint = '#/games/sprint',
  wordsbook = '#/wordsbook',
  games = '#/games',
}

export default Pages;
